---
import {Image} from "astro:assets";

interface Props {
  title: string;
  body: string;
  src: any;
  alt: string;
}

const { src, title, body, alt } = Astro.props;
---

<div
  class="flex items-center justify-start flex-col gap-y-4 bg-white/5 p-8 text-center rounded-[24px]"
>
  <Image src={src} alt={alt} height={200} />
  <h4 class="text-white text-[24px] font-bold">
   {title}
  </h4>
  <p class="text-white opacity-50 text-[16px]">
	{body}
  </p>
</div>
